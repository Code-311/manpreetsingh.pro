@model ToolsPageViewModel
@{
    ViewData["Title"] = Model.Title;
}
<section class="page-intro section-reveal">
    <p class="kicker">Tools</p>
    <h1>@Model.Title</h1>
    <p>@Model.Intro</p>
</section>
<section>
    <div class="card-grid compact">
        @foreach (var item in Model.Items)
        {
            <article class="info-card" tabindex="0">
                <h2><a asp-controller="Home" asp-action="Tool" asp-route-slug="@item.Slug">@item.Name</a></h2>
                <p>@item.Summary</p>
            </article>
        }
    </div>
</section>
