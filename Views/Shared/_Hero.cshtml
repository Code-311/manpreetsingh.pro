@model HomePageViewModel
<section class="hero section-reveal" aria-labelledby="hero-title">
    <div class="meta-row">
        <span>@Model.HeroKicker</span>
        <span>UTC+01</span>
    </div>
    <h1 id="hero-title">@Model.HeroStatement</h1>
    <p class="hero-subline">@Model.HeroSubline</p>
</section>
