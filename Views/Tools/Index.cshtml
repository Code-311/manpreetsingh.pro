@model ListPageViewModel<Tool>
<h1>@Model.Title</h1>
@foreach (var item in Model.Items){<article><h2><a asp-action="Detail" asp-route-slug="@item.Slug">@item.Title</a></h2><p>@item.Summary</p></article>}
